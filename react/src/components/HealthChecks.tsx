import {UserProfile} from "../model/User";
import {ConfigValues} from "../model/Config";

interface IProps {
  user: UserProfile;
  config: ConfigValues;
}

export const HealthChecks = (props: IProps) => {

}
